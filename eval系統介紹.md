# CloudDrive AI 助理 — Eval（測試與驗證）系統介紹

本文件介紹 CloudDrive 內嵌 AI 助理的 eval 系統：它的目標、如何測試、測了什麼、以及結果。
適合作為理解「本專案如何驗證一個本地小模型的可靠度」的入口文件。

---

## 一、目標

**核心目標：以「真實模型」的行為量測，取代「只信單元測試」，來保證 AI 助理的可靠度。**

一個以 LLM 為核心的系統無法只靠傳統單元測試保證正確——單元測試中 LLM 是 mock（假回應），
測試通過只證明「程式管線接得對」，不證明「真實模型做得對」。實例：codegen 加結構化輸出後
615 個單元測試全綠，但真實模型量測顯示產碼品質 0/4（全失敗）。

因此 eval 系統的三個具體目標：
1. **可重複量測**真實 gemma4:26b 的通過率、延遲、跳針率（機率性系統不看單次）。
2. **資料驅動地定位並修正**可靠性問題（量化 → 根因 → 修正 → 再驗證）。
3. **逼出真實使用甚少經過、卻確實有缺陷的角落**（見「結果」中的 recall 案例）。

## 二、系統組成

eval harness 位於 `backend/eval/`，是一個**離線、開發者導向**的工具：它從**外部**呼叫
`/assistant/chat`，把助理當**黑箱**評測，本身不在正式請求路徑上。

**元件**：`schema`（案例定義）→ `runner`（驅動執行）→ `verifier`（斷言）→ `scoring`（評分），
旁掛 `judge`（LLM 考官，質性打分）與 `baseline`（回歸比較）。

**三層測試**：
| 層 | 內容 | 定位 |
|---|---|---|
| 底層 | 確定性單元／整合測試（mock LLM） | CI 快、防管線退化 |
| 中層 | LLM 考官打分 | 輔助之質性評估 |
| 頂層 | 真實模型 eval sweep（gemma4 實跑） | 產出本文件所有量化結果 |

> 註：本文件所有通過率皆由**底層之確定性驗證器**判定，非由考官打分。

**三種執行模式**：API（呼叫 HTTP）、browser（Playwright 驅動真實 UI）、exec（實跑生成的技能驗產出）。

## 三、測試方式

### 3.1 一個測試案例怎麼定義

以 YAML 定義：自然語言 `prompt`、期望（`steps_include` 技能、`requires_confirmation` 確認層級、
`steps_arg_contains`／`reply_contains` 內容）、評分權重、執行次數 `runs`。多輪案例另有
`seed_folders`（跑前播種 drive 狀態）與 `context_turns`（前置對話，串同一 session）。

### 3.2 怎麼「自動判對錯」（機械判定，非模型打分）

驗證器把模型回傳的**結構化 JSON** 做 `==`／`in` 比對。以「改名需確認、計畫須含 rename_item」為例：

```python
status == "pending_approval"           # 是否標記為需確認
"rename_item" in json.dumps(steps)     # 計畫是否含指定技能
"RenamedByAgent" in json.dumps(steps)  # 計畫是否含指定新名
```

相同回應必得相同判定（不似考官打分具變異）。失敗型態「規劃為唯讀」即由此量到：期望
`pending_approval`、實得 `auto_executed`（意即計畫無任何寫入步驟，因唯讀才自動執行）。

### 3.3 量測工具與方法論

- **`temp_sweep`**：對選定案例 × N 次 × 多溫度，量通過率／跳針率／延遲；自起臨時後端、
  每案例前重新登入（避免權杖過期造成假性失敗）。
- **A/B 對照**：疑似退化時另起 baseline 版對打同一請求，取得基準再定位，**不猜**。
- **診斷紀律**：先全量落檔再過濾；對真實 runtime 印出實際回覆，不憑讀碼推論。

## 四、測了什麼（兩條測試線）

### 測試線一：規劃可靠性（防跳針）
量測「模型陷入重複生成（跳針）而停滯／逾時」的比率，並驗證三層防護：
DEC-031（`num_predict` 上限 + 非零溫度＝有界失敗）、DEC-032（技能名 enum＝幻覺技能不可生成）、
DEC-033（`think:false`＝移除跳針棲息的思考段）。

### 測試線二：對話記憶
量測「模型是否真的用了對話歷史」的多輪指涉：純對話指涉、工具結果指涉、嚴謹回想三類案例。

## 五、結果

### 5.1 think:false A/B（代表案例 × 5，僅變思考開／關）
| 組別 | 通過率 | 跳針 | 平均延遲 |
|---|---|---|---|
| thinking 開（對照） | 60% | 3/10 | 92.4 秒 |
| think:false（實驗） | **100%** | **0/10** | **8.6 秒（約快 10 倍）** |

### 5.2 溫度掃描（thinking 開 vs think:false）
| 溫度 | thinking 開 | think:false |
|---|---|---|
| 0.2 | 90% | 100% |
| 0.4 | 90% | 100% |
| 0.6 | 85% | 100% |
| 0.8 | 90% | 100% |

判讀：修跳針前所見之「溫度效應」實為跳針傾向；`think:false` 後各溫度一致 100%，可知溫度非
正確率之直接因素。故 `structured_temperature` 固定於 0.2（打破貪婪迴圈之最小非零溫度）求確定性。

### 5.3 模型能力上限
雜訊清除後，困難集 M3/M5（多查詢 + 一個寫入）通過率仍約 **47%**；失敗典型為「合法但語意錯——
遺漏使用者要求之寫入步驟（規劃為唯讀）」。此屬模型能力限制，機制無法解決。

### 5.4 對話記憶三案例（含一個「eval 發現、真實使用未觸及」的缺陷）
| 案例 | 驗證內容 | 結果 |
|---|---|---|
| `multiturn-create-second` | 自對話文字解「第二個=2023」 | 5/5 |
| `multiturn-rename-first` | 自結果摘要取 item_id 改名（代理） | 5/5 |
| `multiturn-recall-listed-names` | 僅自對話報出全部三名（嚴謹） | 修復前 **0/5** → 修復後 **5/5** |

**回想案例（recall）的閉環——本系統最能展現價值的一段**：
- **情境**：全新對話 →「列出我的資料夾」（列根目錄）→ 下一輪「剛才那些叫什麼名字」。
- **真實使用為何未觸及**：使用者看檔案用 UI，不會叫助理「列根目錄」；「列完再問名字」日常幾乎不做。
  缺陷潛伏於此少走之路徑，直至 eval 刻意執行方暴露——**這正是 eval 的價值**。
- **診斷（方法論實例）**：初判「摘要截斷」是憑讀碼假設、未診斷；依假設修後仍 0/5；印出實際
  runtime 才發現真因是**兩個疊加缺陷**：①模型列根目錄傳 `parent_id="root"` 使 `_optional_uuid`
  崩潰（list_items 失敗、記憶無檔名）；②即使成功，摘要序列化原始 dict（含 UUID）超 200 字被截斷。
- **修復並驗證**：兩者皆修 → recall **0/5 → 5/5**、三案例綜合 100%（15/15）。
- **教訓**：憑讀碼往前推、預設上游成功，是典型認知陷阱；**真實診斷才是可信之判準**。

## 六、系統邊界與限制

**邊界**：eval 系統擁有「案例定義、驅動、判定、評分」；它驅動但不擁有「助理（受測系統）、
真實模型（Ollama）、資料庫、考官」；它以黑箱、離線方式從 HTTP 外部評測，不在請求路徑上。

**不在範圍內**：不訓練／修改模型、不跨模型（綁定 gemma4:26b）、不代表真實使用分佈（合成案例）、
非負載／效能／安全測試。

**限制**：部分 sweep 樣本數 5–10（通過率為趨勢非精確值）；考官具主觀性故僅為輔助。

---

## 附錄 A：測試環境
| 項目 | 內容 |
|---|---|
| 本地模型 | gemma4:26b |
| 推論引擎 | Ollama 0.31.1 |
| 量測工具 | `backend/eval/temp_sweep.py`、`backend/eval/run.py` |
| 資料庫 | PostgreSQL |
| 量測期間 | 2026-07-06 至 2026-07-09 |

## 附錄 B：案例 ID 對照
| 案例 ID | 說明 |
|---|---|
| `storage-quota-read`、`safety-destructive-confirm` | 跳針高危／破壞性確認 |
| `read-only-list`、`create-folder-write` | 穩定唯讀／寫入 |
| `gen-m3-*`、`gen-m5-*` | 困難集（多查詢＋寫入／加步驟引用） |
| `multiturn-create-second`／`-rename-first`／`-recall-listed-names` | 記憶：純對話／工具結果（代理）／嚴謹回想 |

## 附錄 C：原始數據索引（`backend/eval/out/`）
| 實驗 | 原始檔 |
|---|---|
| 溫度掃描（think:false，4 溫×10） | `temp_sweep_20260708T135541Z.md` |
| 溫度掃描（thinking 開，4 溫×5） | `temp_sweep_20260708T165846Z.md` |
| 記憶三案例（recall 0/5） | `temp_sweep_20260708T034239Z.md` |
| 記憶三案例（recall 修復後 5/5） | `temp_sweep_20260709T011132Z.md` |
| think:false A/B | 見 `doc/tasks/assistant-eval.md` §E8 |

> 完整決策與實驗記錄見專案 `doc/decisions.md`（DEC-031~033）與 `doc/tasks/assistant-eval.md`（E1–E8）。
