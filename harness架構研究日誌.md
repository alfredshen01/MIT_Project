# Harness 架構研究日誌

> 研究歷程記錄。\
> 本檔記錄**研究過程本身**：何時遇到什麼現象、提出什麼假設、怎麼實驗、數據說了什麼、如何決策。

---

## 研究概述

- **對象**：CloudDrive 內嵌 AI 助理的 Agent Harness——把本地小模型 gemma4:26b 包裝成可靠代理。
- **核心研究問題**：**一個不夠聰明、不穩定的本地小模型,能不能用工程機制馴服到「可用的可靠度」?**
- **一句話結論**：**能——但要分清「機制救得了的」與「只能靠模型智慧的」;前者用機制根治,後者用資料量測、有限度地推。**

## 時間線總覽

| 期間 | 階段 | 關鍵產出 |
|---|---|---|
| 6/17–6/20 | **測試基礎設施**(eval harness E1–E6) | 400 案例套件、三執行模式、LLM 考官、分數導向 |
| 7/05 | 執行策略決策 | DEC-030 串行執行(DAG 並行延後) |
| 7/06 | **防跳針 循環一、二** | DEC-031(結構化解碼)、DEC-032(enum)、temp_sweep + E7 量測 |
| 7/07 | **防跳針 循環三 + 記憶起步** | codegen 保護、DEC-033(think:false A/B)、記憶 v1、多輪 eval 起步 |
| 7/08 | **記憶收尾 + eval 嚴謹化** | confirm 修復、記憶限制、多輪 eval 結果(recall 0/5 揭露保真限制) |

## 研究方法論（本研究最重要的方法貢獻）

> **量化 → 定位根因 → 修正 → 再驗證**

1. **真模型量測(temp_sweep)**：代表案例 × N 次 × 多溫度,量 pass-rate/跳針率/延遲;機率性系統**不看單次**。
2. **A/B 對照**：疑似退化時起 baseline 版對打,拿基準再定位,**不猜**。
3. **單元測試不足以驗 LLM**：單元裡 LLM 是 mock 的,全綠只證明管線接得對,不證明真模型做得對。
4. **全量 log 落檔再過濾**：診斷先完整記錄,不在管線裡先 tail/grep。

---

## 時間線詳述

### 6/17–6/20 ── 建立測試基礎設施（eval harness）

先有「尺」才能量。這幾天把驗證/評分 harness 從無到有建起來:

- **6/17**：生成 M2–M5 案例套件(每級 100,共 400);M4 自我撰寫案例納入 browser。
- **6/18**：新增 **execution 模式**(實跑生成的 skill、驗產出內容)、**browser 模式**(Playwright 驅動真 UI);建 `eval-prompt-log`;規模擴到 400。
- **6/19**：把 M2 重設計為**真的用滿 5 個查詢工具**的情境(非任意組合);**E6 可換考官 provider**(gemma/codex/openai);**分數導向範式**——考官給分數 + 優缺點,不只 pass/fail。
- **6/20**：`--tag`/`--verbose` CLI;文件與實作對齊。
- **階段意義**：這套 harness 是後面所有「量化」的前提。**沒有可重複的量測,就沒有資料驅動的迭代。**

### 7/05 ── DEC-030：執行維持串行

- **決策**：workflow 執行維持**串行**,DAG 並行延後。
- **原因**：所有技能共用同一 AsyncSession,並行需先解此前置。**誠實記錄「暫不做」與理由,不假裝完整。**

### 7/06 ── 防跳針 循環一、二（DEC-031 / DEC-032）+ E7 量測

**現象**：某些請求(如「查剩餘容量」)讓模型陷入**重複迴圈**卡到 timeout。
**定位**：結構化請求把 temperature 釘 0,gemma4 貪婪解碼下掉入決定性重複迴圈,同 prompt 100% 重現。

- **循環一 DEC-031**：`num_predict` 生成上限(跳針有界失敗)+ 結構化請求用**低而非零**溫度(微量隨機打破貪婪迴圈,格式仍由 grammar 保證)。**驗證**：原卡死的整合測試 40/40 過。
- **同日建 temp_sweep + E7 首次量測**(4 案例 × 5 × 4 溫度)。**首次量化發現:破壞性規劃是模型重災區**(0–40% 可靠度,跳針與幻覺技能並存)。過程還修了兩個量測 bug(token 過期假 401、後端提早退出未偵測)——**工具本身也要驗**。
- **循環二 DEC-032**：模型會規劃出**不存在的技能名**。把計畫 schema 技能名做成 **enum**(依當下 registry 動態組)→ 幻覺技能**取樣層即不可生成**。副發現:部分 400 其實是 `depends_on` 非法相依,`validate_plan` 漏查 → 補上。
- **關鍵洞察**：DEC-032 是「機制優先於模型自覺」教科書例——不是求模型別亂編,是讓亂編**文法上不可能**。

### 7/07 ── 防跳針 循環三（DEC-033）+ codegen 保護 + 記憶起步


**(a) codegen 結構化保護**
- codegen 是最後一個未受 grammar 保護的結構化呼叫點 → 加 `response_format`。
- **踩坑(真模型 A/B)**：把 codegen 拖進結構化的低溫 0.2 → 產碼品質 **0/4 崩盤**(信封完好、碼壞)。修:per-call temperature 覆寫,codegen 用 0.8 全採樣。**原則:temperature 依「任務類型」而非「是否結構化」。**

**(b) 循環三 DEC-033：think:false（治本 + 快 10×）**
- **定位**：DEC-031/032 後殘留跳針,**唯一棲息地是模型的「思考」段**——grammar 只約束最後答案,管不到思考段。
- **think:false A/B(60 樣本,只改思考開/關一個變數)**:

  | | thinking 開 | think:false |
  |---|---|---|
  | 通過率 | 60% | **100%** |
  | 跳針 | 3/10 | **0/10** |
  | 平均延遲 | 92.4s | **8.6s(快 ~10×)** |

- **結論**：思考對這顆小模型的**規劃品質無可量測貢獻,卻是跳針與延遲的全部來源**。→ planner 預設 `think:false`;codegen 不連動。
- **裸露出的天花板**：雜訊清掉後,困難集 M3/M5(多查詢 + 一個寫入)仍僅 **~47%**;失敗典型是**合法但語意錯——漏掉使用者要的寫入步驟(規劃成唯讀)**。歸類為**模型能力限制**(機制救不了)。

**(c) 對話記憶 v1 起步 + 多輪 eval 起步**（詳見 7/08 收尾）

### 7/08 ── 記憶收尾 + 多輪 eval 嚴謹化

**(a) 對話記憶：根因與關鍵實驗**
- **根因(查證非推測)**：對話**有存進 DB、但規劃器只收當前訊息**——歷史從沒回讀。修法是接線。
- **關鍵 A/B(工具結果怎麼承載)**：`tool` 角色 **0/4**(幻覺假檔名,chat template 不消化孤立 tool 訊息)vs assistant 文字 **4/4** → 以 assistant 文字承載,**順帶零 migration**。
- **工程 bug(使用者實測抓到)**：「確認執行成功」後問「剛剛怎麼樣」→ 答「還在等確認」。根因:記憶只在 `/chat` 寫歷史,手動確認走 `/confirm` **從沒寫回**。修:`/confirm` 執行後把結果摘要寫回 session。

**(b) 多輪 eval 嚴謹化**
- 把單輪 harness 擴成多輪(播種狀態 + 串 session),加指涉案例。
- **結果**：純對話指涉 5/5、工具結果指涉(代理)5/5、**嚴謹回想 0/5**。
- **0/5 的價值(誠實發現)**：非管線壞,是**摘要保真不足**——`summarise_results` 對 list_items 用 `str(原始 dict)`(含 UUID)→ 三項超 200 字截斷 → 只留第一名。**代理測試遮住的,嚴謹測試抓到。**

---

## 跨研究線的洞察（本研究沉澱出的兩把判斷尺）

1. **機制 vs 模型自覺(設計時用)**：能用文法/程式保證的,不靠模型自律。
2. **模型限制 vs 工程缺口(排序時用)**：工程缺口做一次永久解決(值得做);模型限制沒機制能保證、只能推(有限投入)。

## 未解問題與未來方向

- **記憶保真**(工程,可解)：改摘要格式(集合型只萃取關鍵欄位)→ 重跑 recall。
- **斷線取消**(工程,可解)：放棄的請求仍佔 Ollama 單併發槽(真實使用發現)。
- **planner 寫入意圖**(模型前沿,推非解)：用多輪 eval 迭代 prompt 工程。
- **記憶 v2**：摘要壓縮 → 語意檢索(復用 pgvector)→ 跨 session。

## 研究反思（方法論層面,最該記住的）

> **最大的認知風險不是「不會做」,而是「以為驗證過了」。** 兩週內至少三次「All pass」但使用模型實測及真實使用發現有問題。
>
> **對策不是寫更多同源測試(它們有一樣的盲點),而是引入不同源的視角**:真模型 A/B、機械式窮舉資料表面。
